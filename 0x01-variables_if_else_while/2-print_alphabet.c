#include <stdio.h>

/**
 * main - print if insomnia and fall asleep
 *
 * Return: Always (Success)
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);

	putchar('\n');
	return (0);
	
}
